import{expect,test}from"@playwright/test";
test("builds, shares and exports a planting note",async({page})=>{await page.goto("/");await expect(page.getByRole("heading",{name:/Plan a patch/})).toBeVisible();await page.getByLabel("Where is the garden?").fill("Calgary");await page.getByRole("button",{name:/Compose my planting note/}).click();await expect(page.locator("#your-plan")).toBeVisible();await expect(page.getByRole("heading",{name:"Calgary"})).toBeVisible();const download=page.waitForEvent("download");await page.getByRole("button",{name:/Save PDF/}).click();expect((await download).suggestedFilename()).toContain("acrekind-calgary")});
test("does not overflow a phone viewport",async({page})=>{await page.setViewportSize({width:390,height:844});await page.goto("/");expect(await page.evaluate(()=>document.documentElement.scrollWidth<=document.documentElement.clientWidth)).toBe(true)});
